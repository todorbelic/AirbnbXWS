namespace AccommodationService.Enums
{
    public enum NotificationType
    {
        RESERVATION_REQUEST,
        RESERVATION_CANCEL,
        HOST_RATE,
        ACCOM_RATE,
        GOT_FEATURED_HOST,
        LOST_FEATURED_HOST,
        GUEST_ACCEPTED
    }
}
